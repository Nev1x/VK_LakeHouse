# File-based password authenticator [T6, FR-015]
# password.db генерируется при bootstrap из TRINO_USER/TRINO_PASSWORD (bcrypt), в git не попадает.
password-authenticator.name=file
file.password-file=/etc/trino/auth/password.db
