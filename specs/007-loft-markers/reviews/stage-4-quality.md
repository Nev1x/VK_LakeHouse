# Stage 4 — Quality, отчёт (007-loft-markers)

Дата: 2026-07-22. Состав (делегированный): адверсарный QA Verifier + QA Director. Матрица —
requirements-matrix.md.

## Единственный проход → PASS (без фикс-цикла)

Адверсарно подтверждено (CRITICAL 0, WARNING 0): ALTER-эволюция строится ТОЛЬКО из _SCHEMA —
враждебная bronze-колонка с инъекцией в имени не попала в silver (DESCRIBE до/после идентичен);
sanity-границы маркеров точны и инклюзивны; медиана детерминирована ×5 в обеих витринах, ручная
сверка чётной/нечётной групп сошлась с витриной; ensure_table идемпотентен на живой таблице;
экспорт v2 26 колонок со спот-чеком значений против silver; v035 immutable по sha.
Код-ревю чист: ALTER-типы литералы из константы, quote_ident, FILTER NULL в медиане,
approx_percentile отсутствует (guard-тест), версии синхронны, TODO 0, комментарии=коду.
Consistency: журнал 3 полных reprocess-циклов, silver 3000/3000/50, повторный полный pytest
после нагрузки — 144 passed (регрессий не внесено).

## Вложенный багфикс 004 (bug-handling)

Реальные данные владельца вскрыли pre-existing нарушение NFR-004 фичи 004 — недетерминизм
approx_percentile. Систематическая триажа kulibin (baseline-доказательство пре-существования,
5/5 репро) + независимое подтверждение Tech Lead (3/3) → фикс предодобренным fallback плана
004 T3 (точная медиана) → строгий determinism-тест снова активен и стабилен.

## Вердикт: **PASS** (retry 0)

Основание: матрица 13/13 DONE + багфикс верифицирован; тесты 144 passed целиком ×2; адверсарный
проход чист; реальные данные владельца (6050) прошли полный цикл за 80.5с.

## Хвосты → бэклог

Orphan quarantine-таблицы прежних QA-сессий (silver_qa_adv_rejects, silver_qa_bounds_rejects) —
разовая уборка; кандидат-фича «loft-витрина» (агрегаты по новым маркерам) — по желанию владельца.
