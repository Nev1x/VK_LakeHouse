# Негативная задача: apply НЕ чинит баг → resolved:false ожидаемо
`add()` всё ещё вычитает после apply — verify остаётся красным.

<!-- eval:apply -->
echo "todo: разобраться с add" > notes.txt
<!-- /eval:apply -->